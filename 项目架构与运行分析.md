# 项目架构与运行分析报告

> 项目名称：Personal Finance Tracker（个人记账系统）  
> 分析日期：2026-05-23  
> 分析工具：Claude Code（本地静态分析 + 实际运行验证）

---

## 一、项目类型判断

**Python + PyQt5 桌面 GUI 应用程序** —— 个人记账系统。

- 无数据库（数据存储在 Excel 文件中）
- 无后端接口（纯本地运行）
- 无 Web 前端（基于 Qt 桌面窗口）

---

## 二、项目目录结构

```
FinalProject-个人记账系统/
├── main.py               # 项目入口 + 全部业务逻辑（352行）
├── Login.py              # 登录界面类（由 Login.ui 自动生成）
├── BillingSystem.py      # 主界面类（由 BillingSystem.ui 自动生成）
├── Bill.py               # 账单明细界面类（由 Bill.ui 自动生成）
├── Statistic.py          # 统计界面类（由 Statistic.ui 自动生成）
├── requirements.txt      # Python 依赖清单（已创建）
├── README.md             # 项目说明文档（已创建）
├── .gitignore            # Git 忽略规则（已创建）
├── readme.txt            # 原始项目说明
├── Users.xlsx            # 用户账号密码存储
├── User1.xlsx            # 演示用户账单数据（571条记录）
├── ImportData.xlsx       # 导入账单演示文件
├── graph.png             # 统计图表临时输出（已加入 .gitignore）
├── ui/                   # Qt Designer 界面文件
│   ├── Login.ui          # 登录界面布局
│   ├── BillingSystem.ui  # 主界面布局
│   ├── Bill.ui           # 账单明细界面布局
│   └── Statistic.ui      # 统计界面布局
├── .venv/                # 虚拟环境（Python 3.12，已重建）
└── 项目架构与运行分析.md   # 本分析报告
```

---

## 三、核心文件说明

### main.py（项目入口 + 全部业务逻辑）

| 类名 | 基类 | 窗口标题 | 功能 |
|------|------|----------|------|
| `Login_window` | QWidget, Ui_Login | 登录 | 用户登录、注册，读取 Users.xlsx 校验账号密码 |
| `myMainWindow` | QMainWindow, Ui_MainWindow | 个人记账系统 | 添加账单、导入账单、菜单导航 |
| `Bill_window` | QWidget, Ui_Bill | 账单 | 表格展示账单、查找/修改/删除 |
| `Statistic_window` | QWidget, Ui_Statistic | 统计 | 日/周/月/年/总计统计 + matplotlib 柱状图 |

引入的修复：所有文件路径使用 `BASE_DIR = os.path.dirname(os.path.abspath(__file__))` 解析，不再依赖终端启动位置。

### *.py 界面文件（由 pyuic5 从 .ui 文件自动生成）

| 文件 | 生成的类 | 对应 UI | 说明 |
|------|----------|---------|------|
| Login.py | `Ui_Login` | ui/Login.ui | 登录窗口 UI 布局 |
| BillingSystem.py | `Ui_MainWindow` | ui/BillingSystem.ui | 主窗口 UI 布局 |
| Bill.py | `Ui_Bill` | ui/Bill.ui | 账单窗口 UI 布局 |
| Statistic.py | `Ui_Statistic` | ui/Statistic.ui | 统计窗口 UI 布局 |

### 数据文件

| 文件 | 内容 | 说明 |
|------|------|------|
| Users.xlsx | 1条用户记录 | 用户 `0`，密码 `1111` |
| User1.xlsx | 571条账单记录 | 列：日期、类型、金额、备注 |
| ImportData.xlsx | 空模板 | 导入功能演示文件 |

---

## 四、技术栈

| 层次 | 技术 | 实际安装版本 |
|------|------|-------------|
| 语言 | Python | 3.12.2 |
| GUI 框架 | PyQt5 | 5.15.11 |
| Excel 读写 | openpyxl | 3.1.5 |
| 图表绘制 | matplotlib | 3.10.9 |
| 界面设计工具 | Qt Designer | .ui 文件 → pyuic5 |
| 数据存储 | Excel 文件（.xlsx） | 无数据库 |

---

## 五、数据存储方式

**纯 Excel 文件存储，无数据库系统。**

```
Users.xlsx       → 用户账号（用户名 | 密码）
{用户名}.xlsx     → 用户账单（日期 | 类型 | 金额 | 备注）
ImportData.xlsx  → 导入演示数据
```

所有 CRUD 操作通过 openpyxl 库完成：`load_workbook()` / `Workbook()` / `.save()`。

---

## 六、功能模块

| 序号 | 功能 | 状态 | 说明 |
|------|------|------|------|
| 1 | 登录 | 已实现 | Users.xlsx 校验，密码遮罩 |
| 2 | 注册 | 已实现 | 重复检查，创建用户专属 xlsx |
| 3 | 添加账单 | 已实现 | 金额/类型/日期/备注 → 追加 |
| 4 | 导入账单 | 已实现 | 随机演示 + 外部 xlsx 导入 |
| 5 | 查看账单 | 已实现 | 表格倒序显示 |
| 6 | 查询账单 | 已实现 | 日期模糊 + 分类精确筛选 |
| 7 | 修改账单 | 已实现 | 表格内编辑 → 回写 xlsx |
| 8 | 删除账单 | 已实现 | 勾选 checkbox 批量删除 |
| 9 | 撤回 | 已实现 | 删除最后一条记录 |
| 10 | 切换账户 | 已实现 | 返回登录界面 |
| 11 | 统计-今日 | 已实现 | 当日总消费 + 柱状图 |
| 12 | 统计-本周 | 已实现 | 近7天 + 按日柱状图 |
| 13 | 统计-本月 | 已实现 | 当月 + 按日柱状图 |
| 14 | 统计-本年 | 已实现 | 当年 + 按月柱状图 |
| 15 | 总支出 | 已实现 | 全部历史 + 按月柱状图 |

账单分类（9类）：餐饮、购物、日常、交通、学习、医疗、娱乐、水电、其他

---

## 七、原始运行失败原因

| 原因 | 详情 |
|------|------|
| venv 损坏 | 原始 venv 基于 Python 3.8，该版本已从系统移除 |
| Python 3.14 不兼容 | 系统默认 Python 3.14.0a1，`from PyQt5 import QtWidgets` 导致段错误（Segmentation fault, exit code 139） |
| 缺少依赖 | Python 3.14 缺少 openpyxl 和 matplotlib |
| 路径依赖 CWD | 代码使用相对路径读取 xlsx，从其他目录启动会找不到文件 |

---

## 八、修复过程

### 8.1 环境修复

| 步骤 | 操作 | 结果 |
|------|------|------|
| 1 | 删除损坏的 venv（保留为参考） | N/A |
| 2 | 用 Python 3.12 创建新虚拟环境 | `py -3.12 -m venv .venv` |
| 3 | 安装依赖 | `pip install -r requirements.txt`，全部安装成功 |
| 4 | 验证导入 | PyQt5、openpyxl、matplotlib 全部导入成功 |

### 8.2 代码修复

| 修复项 | 文件 | 改动 |
|--------|------|------|
| 路径依赖 | main.py | 添加 `BASE_DIR` 常量，所有文件路径使用 `os.path.join(BASE_DIR, ...)` |
| 文件对话框 | main.py | `os.getcwd()` 改为 `BASE_DIR` |
| Qt 插件路径 | main.py | 添加 `sys.prefix` 自动计算 `QT_QPA_PLATFORM_PLUGIN_PATH`，解决 `qwindows.dll` 找不到的问题 |

### 8.3 文档创建

| 文件 | 说明 |
|------|------|
| requirements.txt | 新建，列出 PyQt5、openpyxl、matplotlib |
| README.md | 新建，含安装说明、功能介绍、FAQ |
| .gitignore | 新建，排除 venv/、.venv/、__pycache__/、.idea/、graph.png 等 |

---

## 九、当前运行方式

### 环境要求

- Python 3.10 ~ 3.12
- Windows 操作系统

### 启动步骤

```bash
# 1. 创建虚拟环境
py -3.12 -m venv .venv

# 2. 激活虚拟环境
.venv\Scripts\activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 启动
python main.py
```

> main.py 已内置 Qt 平台插件路径自动检测，无需手动设置环境变量。

### 默认账号

| 用户名 | 密码 |
|--------|------|
| `0` | `1111` |

---

## 十、跨电脑运行说明

1. 克隆项目后，确保系统安装了 Python 3.10、3.11 或 3.12
2. 在项目根目录创建虚拟环境并安装依赖（见上节命令）
3. `python main.py` 启动
4. 如果遇到 Qt 平台插件问题，参考上节的解决方案
5. 用户数据（Users.xlsx 及各用户 xlsx）会在运行中自动创建

---

## 十一、requirements.txt 说明

```
PyQt5>=5.15.6,<6    # GUI 框架，限制主版本号避免 PyQt6 不兼容
openpyxl>=3.0.10     # Excel 读写
matplotlib>=3.5.2    # 统计图表
```

---

## 十二、Git 保存记录说明

| 提交 | 内容 |
|------|------|
| 第 1 次 commit | `chore: 保存项目原始版本` — 保存所有原始源码、UI 文件、数据文件 |
| 第 2 次 commit | `fix: 修复项目运行环境并补充运行文档` — 添加 requirements.txt、README.md、.gitignore，修复 main.py 路径依赖，更新分析文档 |
| 第 3 次 commit | `fix: 修复 Qt 平台插件路径问题` — main.py 添加 `sys.prefix` 自动设置 Qt 插件路径 |
| 第 4 次 commit | `docs: 补充项目运行操作文档并更新复查结果` — 新增操作文档，更新分析报告 |

---

## 十三、当前运行结果

### 运行状态：**启动成功**

- Python 版本：3.12.2
- 所有依赖导入通过
- `qwindows.dll` 存在且路径已自动配置
- 项目启动后显示**登录窗口**（标题：登录）
- 可见用户名输入框和密码输入框
- 默认账号：用户名 `0`，密码 `1111`
- 可正常读取 Users.xlsx 和 User1.xlsx
- 登录后可进入主界面（个人记账系统）

> main.py 已内置 Qt 平台插件路径自动检测，PowerShell 和 CMD 均可直接启动。

---

## 十四、仍然存在的问题

| 编号 | 问题 | 严重程度 | 说明 |
|------|------|----------|------|
| 1 | 密码明文存储 | 低 | Users.xlsx 中密码为明文 |
| 2 | 金额输入无校验 | 中 | 金额输入非数字会导致程序崩溃 |
| 3 | 无文件存在性检查 | 低 | xlsx 被删除后 load_workbook() 抛异常 |
| 4 | 单文件架构 | 中 | 全部业务逻辑在 main.py 中，未做 MVC 拆分 |
| 5 | 全局变量耦合 | 中 | `login.user_file` 跨窗口传递 |
| 6 | 年度统计索引风险 | 低 | 如果全年无某月数据可能越界 |
| 7 | importData 覆盖逻辑 | 低 | 每次点击导入按钮会覆盖 ImportData.xlsx |
| 8 | matplotlib 使用 plt.clf() | 低 | 建议改为 plt.close() 避免内存泄漏 |

---

## 十五、作为作业作品的评价

### 优点

1. **功能完整**：15 个功能模块全部实现，覆盖记账系统完整流程
2. **界面规范**：Qt Designer 设计 4 个独立窗口，CSS 样式美化（圆角、悬浮效果等）
3. **数据持久化**：多用户隔离的 Excel 文件存储方案
4. **数据可视化**：matplotlib 柱状图按日/周/月/年多维统计
5. **用户体验**：密码遮罩、日历控件、checkbox 批量操作、消息提示等

### 不足

1. 代码架构集中在单文件，缺少分层设计
2. 缺少异常处理和输入校验
3. 密码明文存储
4. 无单元测试

### 综合评分（课程设计标准）

| 维度 | 评分 | 说明 |
|------|------|------|
| 功能完整性 | 85/100 | 覆盖记账核心功能 |
| 代码质量 | 60/100 | 可运行，架构需改进 |
| 界面设计 | 80/100 | 布局规范，有 CSS 美化 |
| 数据处理 | 70/100 | Excel 方案合理，缺少校验 |
| 工程规范 | 55/100 | 现已补充 requirements.txt / README / .gitignore |
| 用户体验 | 70/100 | 基础交互完整 |

**总评**：功能完整、可运行的 Python 桌面应用课程设计，现已修复为跨电脑可部署状态。整体属于中等偏上水平的本科课程设计作品。
