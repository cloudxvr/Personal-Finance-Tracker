# 项目架构与运行分析报告

> 项目名称：个人记账系统  
> 分析日期：2026-05-23  
> 分析工具：Claude Code（本地静态分析 + 实际运行验证）

---

## 一、项目类型判断

**Python + PyQt5 桌面 GUI 应用程序** —— 个人记账系统。

- 无数据库（数据存储在 Excel 文件中）
- 无后端接口（纯本地运行）
- 无 Web 前端（基于 Qt 桌面窗口）

---

## 二、项目目录结构

```
FinalProject-个人记账系统/
├── main.py              # 项目入口 + 全部业务逻辑（348行）
├── Login.py             # 登录界面类（由 Login.ui 自动生成）
├── BillingSystem.py     # 主界面类（由 BillingSystem.ui 自动生成）
├── Bill.py              # 账单明细界面类（由 Bill.ui 自动生成）
├── Statistic.py         # 统计界面类（由 Statistic.ui 自动生成）
├── readme.txt           # 项目说明文件
├── Users.xlsx           # 用户账号密码存储
├── User1.xlsx           # 演示用户账单数据（571条记录）
├── ImportData.xlsx      # 导入账单演示文件
├── graph.png            # 统计图表临时输出图片
├── ui/
│   ├── Login.ui         # 登录界面布局（Qt Designer 设计）
│   ├── BillingSystem.ui # 主界面布局
│   ├── Bill.ui          # 账单明细界面布局
│   └── Statistic.ui     # 统计界面布局
└── venv/                # 虚拟环境（Python 3.8，原始 Python 已不存在，已损坏）
```

---

## 三、核心文件说明

### main.py（项目入口 + 全部业务逻辑）

| 类名 | 基类 | 窗口标题 | 功能 |
|------|------|----------|------|
| `Login_window` | QWidget, Ui_Login | 登录 | 用户登录、注册，读取 Users.xlsx 校验账号密码 |
| `myMainWindow` | QMainWindow, Ui_MainWindow | 个人记账系统 | 添加账单、导入账单、菜单导航（查看账单/统计/撤回/切换账户） |
| `Bill_window` | QWidget, Ui_Bill | 账单 | 表格展示账单明细、按日期/分类查询、修改保存、勾选删除 |
| `Statistic_window` | QWidget, Ui_Statistic | 统计 | 今日/本周/本月/本年/总支出统计 + matplotlib 柱状图 |

### *.py 界面文件（由 pyuic5 从 .ui 文件自动生成）

| 文件 | 生成的类 | 对应 UI | 说明 |
|------|----------|---------|------|
| Login.py | `Ui_Login` | ui/Login.ui | 登录窗口布局：用户名/密码输入框、登录/注册按钮 |
| BillingSystem.py | `Ui_MainWindow` | ui/BillingSystem.ui | 主窗口布局：金额/类型/日期/备注表单、保存/导入按钮、菜单栏 |
| Bill.py | `Ui_Bill` | ui/Bill.ui | 账单窗口布局：5列表格（日期/类型/金额/备注/操作）、查找/保存/删除按钮 |
| Statistic.py | `Ui_Statistic` | ui/Statistic.ui | 统计窗口布局：5个统计按钮、总计文本框、图表显示区域 |

### 数据文件

| 文件 | 内容 | 说明 |
|------|------|------|
| Users.xlsx | 1条用户记录 | 列：用户名、密码。当前数据：用户 `0`，密码 `1111` |
| User1.xlsx | 571条账单记录 | 列：日期、类型、金额、备注。演示数据 |
| ImportData.xlsx | 空数据 | 导入功能的演示文件，程序运行时会生成随机数据 |

### readme.txt

项目说明文件，内容摘要：
- 演示账号：User1 / 1111
- 每个用户账单存储在同名 xlsx 文件中
- 需安装的库：openpyxl, matplotlib, PyQt5

---

## 四、技术栈

| 层次 | 技术 | 版本 |
|------|------|------|
| 语言 | Python | 3.8（原始 venv） |
| GUI 框架 | PyQt5 | 5.15.6 |
| Excel 读写 | openpyxl | 3.0.10 |
| 图表绘制 | matplotlib | 3.5.2 |
| 数值计算 | numpy | 1.22.4 |
| 数据处理 | pandas | 1.4.2 |
| 图像处理 | Pillow | 9.1.1 |
| 界面设计工具 | Qt Designer | .ui 文件 → pyuic5 生成 .py |
| 数据存储 | Excel 文件（.xlsx） | 无数据库 |
| 构建打包 | 无 | 项目内未发现 setup.py / pyproject.toml |

### 第三方依赖（从 venv site-packages 确认）

```
PyQt5==5.15.6
openpyxl==3.0.10
matplotlib==3.5.2
numpy==1.22.4
pandas==1.4.2
Pillow==9.1.1
```

> 项目内未发现 requirements.txt 文件。

---

## 五、数据存储方式

**纯 Excel 文件存储，无数据库系统。**

### 存储结构

```
Users.xlsx          → 用户账号信息（用户名 | 密码）
{用户名}.xlsx        → 用户账单数据（日期 | 类型 | 金额 | 备注）
ImportData.xlsx      → 导入功能的演示数据/导入模板
```

### 数据操作方式

所有 CRUD 操作通过 openpyxl 库完成：

| 操作 | 代码方式 |
|------|----------|
| 创建用户 | `Workbook().save("{user}.xlsx")` |
| 添加账单 | `sheet.append([date, type, money, remark])` + `wb.save()` |
| 查看账单 | 倒序遍历 `sheet`，逐行写入 QTableWidget |
| 修改账单 | 直接在 QTableWidget 编辑 → `sheet.cell().value = xxx` 回写 |
| 删除账单 | 勾选 checkbox → `sheet.delete_rows()` → `wb.save()` |
| 查询账单 | 遍历 sheet，按日期（模糊）和分类（精确）过滤 |
| 导入账单 | 读取外部 xlsx，逐行 append 到用户 xlsx |

---

## 六、功能模块

| 序号 | 功能模块 | 状态 | 实现说明 |
|------|----------|------|----------|
| 1 | **登录** | 已实现 | 读取 Users.xlsx 逐行匹配用户名密码；密码框设为 EchoMode.Password |
| 2 | **注册** | 已实现 | 检查重复 → 写入 Users.xlsx → 创建用户专属 xlsx；注册后直接进入主界面 |
| 3 | **添加账单** | 已实现 | 主界面表单：金额、类型（9类下拉）、日期（日历控件）、备注 → 追加到用户 xlsx |
| 4 | **导入账单** | 已实现 | 生成随机演示数据到 ImportData.xlsx / 通过 QFileDialog 选择外部 xlsx 导入 |
| 5 | **查看账单** | 已实现 | 表格按倒序显示所有记录，每行含日期/类型/金额/备注 + 操作 checkbox |
| 6 | **查询账单** | 已实现 | 按日期模糊匹配 + 分类精确匹配筛选，结果显示在表格中 |
| 7 | **修改账单** | 已实现 | 表格内直接编辑 → 点击保存按钮 → 逐格比对后回写 xlsx |
| 8 | **删除账单** | 已实现 | 勾选 checkbox → 点击删除按钮 → 批量从表格和 xlsx 中移除 |
| 9 | **撤回** | 已实现 | 菜单栏"记账 → 撤回"：删除 xlsx 最后一行 |
| 10 | **切换账户** | 已实现 | 菜单栏"用户 → 切换账户"：返回登录界面 |
| 11 | **今日统计** | 已实现 | 当日消费总额 + 每笔金额柱状图 |
| 12 | **本周统计** | 已实现 | 近7天消费总额 + 按日汇总柱状图 |
| 13 | **本月统计** | 已实现 | 当月消费总额 + 按日汇总柱状图 |
| 14 | **本年统计** | 已实现 | 当年消费总额 + 按月汇总柱状图 |
| 15 | **总支出统计** | 已实现 | 全部历史消费总额 + 按月汇总柱状图 |

### 账单分类（共9类）

餐饮 | 购物 | 日常 | 交通 | 学习 | 医疗 | 娱乐 | 水电 | 其他

---

## 七、启动方式

### 预期启动方式

```bash
# 步骤1：激活虚拟环境
.\venv\Scripts\activate

# 步骤2：运行项目
python main.py
```

### 登录凭据

| 来源 | 用户名 | 密码 |
|------|--------|------|
| readme.txt 说明 | User1 | 1111 |
| Users.xlsx 实际数据 | 0 | 1111 |

> 注意：readme.txt 称演示账号为 User1，但 Users.xlsx 中实际存储的用户名为 `0`。两者均使用密码 `1111`。

---

## 八、当前运行结果

### 运行状态：**无法启动**

尝试运行命令：`python main.py`（使用系统 Python 3.14.0a1）

### 报错/退出信息

```
Segmentation fault (exit code 139)
```

### 失败原因分析

| 序号 | 原因 | 详情 |
|------|------|------|
| 1 | **venv 已损坏** | venv 基于 Python 3.8（路径 `C:\Users\15296\AppData\Local\Programs\Python\Python38\python.exe`），该 Python 版本已从系统中移除，venv 无法使用 |
| 2 | **系统 Python 3.14 缺少依赖** | 系统当前仅有的 Python 3.14.0a1 缺少 `openpyxl` 和 `matplotlib` |
| 3 | **PyQt5 二进制不兼容** | Python 3.14 上已安装 PyQt5，但 `from PyQt5 import QtWidgets` 导致段错误——PyQt5 的 C 扩展（.pyd）是为其他 Python 版本编译的，与 Python 3.14 ABI 不兼容 |

### 验证记录

```
$ python --version
Python 3.14.0a1

$ python -c "import PyQt5; print('OK')"
OK

$ python -c "from PyQt5 import QtWidgets"
Segmentation fault (exit code 139)

$ python -c "import openpyxl"
ModuleNotFoundError: No module named 'openpyxl'

$ python -c "import matplotlib"
ModuleNotFoundError: No module named 'matplotlib'
```

### 系统中可用的 Python 版本

```
Python 3.14 (64-bit)   ← 当前默认，缺少依赖，PyQt5 不兼容
Python 3.12 (64-bit)   ← 缺少全部三个依赖
Python 3.9  (64-bit)   ← 缺少全部三个依赖
```

---

## 九、发现的问题

### 9.1 环境与配置问题

| 编号 | 问题 | 严重程度 | 说明 |
|------|------|----------|------|
| E1 | venv 损坏 | **严重** | 原始 Python 3.8 已卸载，venv 完全不可用 |
| E2 | 缺少 requirements.txt | 中等 | 依赖仅在 readme.txt 中用一行文字说明，不符合 Python 项目标准 |
| E3 | Python 版本兼容性 | **严重** | PyQt5 5.15.6 不支持 Python 3.14，需使用 Python 3.8~3.11 |

### 9.2 代码架构问题

| 编号 | 问题 | 说明 |
|------|------|------|
| C1 | 单文件架构 | 所有 4 个窗口类的完整业务逻辑集中在 main.py（348行），Login.py/Bill.py/BillingSystem.py/Statistic.py 仅含 UI 布局代码 |
| C2 | 全局变量耦合 | 通过 `login.user_file` 全局变量在不同窗口间传递当前用户名，耦合度高、不符合封装原则 |
| C3 | 无 MVC 分层 | 界面、业务逻辑、数据访问全部混合在一起 |

### 9.3 数据安全与校验

| 编号 | 问题 | 说明 |
|------|------|------|
| S1 | 密码明文存储 | Users.xlsx 中密码以明文保存，无任何加密或哈希处理 |
| S2 | 金额输入无校验 | `int(sheet.cell(i, 3).value)` 假设金额为整数，输入非数字会导致程序崩溃 |
| S3 | 无文件存在检查 | 代码假设 xlsx 文件始终存在，文件被删除后 `load_workbook()` 会抛出异常 |

### 9.4 功能 Bug

| 编号 | 问题 | 位置 | 说明 |
|------|------|------|------|
| B1 | importData 覆盖数据 | main.py:103-108 | 每次点击导入都会重新生成随机数据覆盖 ImportData.xlsx，而非追加 |
| B2 | 统计图表内存泄漏 | main.py:329-341 | 使用 `plt.clf()` 而非 `plt.close()`，且每次保存到同一个 graph.png，连续统计时可能导致图表重叠 |
| B3 | 年度统计索引越界风险 | main.py:304 | `y_axis[date2[1]-1]` 在月份不连续时（如某月无数据）会导致 IndexError |
| B4 | 日期存储为字符串 | main.py:88 | `dateEdit.date().toString(Qt.ISODate)` 将日期存为字符串而非 Excel 日期类型 |
| B5 | 注册后直接进入 | main.py:67-69 | 注册后不要求重新登录，直接用新账号进入系统 |

---

## 十、作为作业作品的评价

### 优点

1. **功能完整度高**：实现了登录、注册、账单增删改查、导入、多维统计（日/周/月/年/全部）等 15 个功能点，覆盖面广。
2. **界面设计规范**：使用 Qt Designer 设计了 4 个独立窗口，布局整齐，运用了微软雅黑字体、圆角按钮、悬浮变色、输入框聚焦效果等 CSS 样式美化。
3. **数据持久化正确**：通过 Excel 文件实现了多用户隔离存储，每个用户拥有独立的账单文件。
4. **数据可视化**：利用 matplotlib 实现了柱状图统计展示，支持按时间维度聚合。
5. **用户体验细节**：密码遮罩输入、日历弹窗选择日期、checkbox 批量删除、QMessageBox 操作提示等。
6. **菜单栏设计**：主窗口有完整的菜单栏（记账/账单/用户），功能入口清晰。

### 不足

1. **代码架构**：所有业务逻辑集中在 main.py 单一文件中，缺少模块化设计。界面生成代码（.py）与业务逻辑分离了，但分离的是 UI 布局而非逻辑层。
2. **健壮性差**：缺少异常处理，输入非数字金额、文件不存在等场景均会导致程序崩溃。
3. **工程规范**：无 requirements.txt、无注释、无类型标注、无测试用例。
4. **安全性**：密码明文存储，无任何保护措施。
5. **Bug 隐患**：年度统计索引计算存在越界风险，importData 逻辑存在覆盖问题，matplotlib 图表存在内存泄漏。
6. **Python 版本紧跟不足**：原始开发使用 Python 3.8（2020年发布），当前系统已升级至 Python 3.14，导致依赖不兼容。

### 综合评分（课程设计标准）

| 维度 | 评分 | 说明 |
|------|------|------|
| 功能完整性 | 85/100 | 覆盖了记账系统的核心功能 |
| 代码质量 | 60/100 | 可运行但架构需改进，缺少异常处理 |
| 界面设计 | 80/100 | 布局规范，有基本的样式美化 |
| 数据处理 | 70/100 | Excel 存储方案合理，但缺少数据校验 |
| 工程规范 | 45/100 | 缺少 requirements.txt、注释、测试 |
| 用户体验 | 70/100 | 基础交互完整，有提示信息 |

**总评**：这是一个功能完整、可演示的 Python 桌面应用课程设计作品。代码实现了记账系统的核心需求，UI 设计较为用心。在架构设计、异常处理、工程规范方面有较大提升空间。整体属于中等偏上水平的本科课程设计作品。
