# Personal Finance Tracker 项目运行操作文档

> 最后更新：2026-05-23  
> 适用系统：Windows

---

## 1. 项目简介

这是一个 Python + PyQt5 的个人记账系统桌面应用（Personal Finance Tracker）。

- 纯本地运行，无需网络
- 数据存储在 Excel 文件中，无需数据库
- 支持多用户、账单增删改查、Excel 导入、多维度统计图表

---

## 2. 项目运行环境

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 / 11 |
| Python 版本 | **推荐 Python 3.10 / 3.11 / 3.12** |
| 不推荐版本 | Python 3.14 — PyQt5 兼容性可能存在问题 |
| 依赖管理 | requirements.txt（pip 安装） |

---

## 3. 项目目录说明

```
项目根目录/
├── main.py               # 项目入口，全部业务逻辑
├── Login.py              # 登录界面（由 Login.ui 自动生成）
├── BillingSystem.py      # 主界面（由 BillingSystem.ui 自动生成）
├── Bill.py               # 账单明细界面（由 Bill.ui 自动生成）
├── Statistic.py          # 统计界面（由 Statistic.ui 自动生成）
├── requirements.txt      # Python 依赖清单
├── README.md             # 项目说明
├── 项目架构与运行分析.md   # 详细架构分析
├── 项目运行操作文档.md     # 本文档
├── .gitignore            # Git 忽略规则
├── Users.xlsx            # 用户账号数据（用户名、密码）
├── User1.xlsx            # 演示账单数据（571条记录）
├── ImportData.xlsx       # 导入功能演示文件
├── ui/                   # Qt Designer 界面文件
│   ├── Login.ui
│   ├── BillingSystem.ui
│   ├── Bill.ui
│   └── Statistic.ui
└── .venv/                # 虚拟环境（需自行创建）
```

---

## 4. 首次运行步骤（从零开始）

### 4.1 确保已安装 Python 3.10 / 3.11 / 3.12

在终端执行以下命令检查已安装的 Python 版本：

```powershell
py --list
```

如果显示 `-V:3.12` 或 `-V:3.11` 或 `-V:3.10`，说明已安装对应版本。

### 4.2 创建虚拟环境

```powershell
cd <项目根目录>
py -3.12 -m venv .venv
```

如果系统没有 Python 3.12，可替换为已安装的版本：

```powershell
py -3.11 -m venv .venv
# 或
py -3.10 -m venv .venv
```

### 4.3 激活虚拟环境

```powershell
.venv\Scripts\activate
```

激活成功后，终端提示符前会出现 `(.venv)` 标识。

### 4.4 升级 pip 并安装依赖

```powershell
python -m pip install --upgrade pip
pip install -r requirements.txt
```

### 4.5 启动项目

```powershell
python main.py
```

---

## 5. 已有虚拟环境时的启动方式

如果已经创建过 `.venv`：

```powershell
cd <项目根目录>
.venv\Scripts\activate
python main.py
```

或者不激活虚拟环境，直接使用 venv 中的 Python：

```powershell
cd <项目根目录>
.venv\Scripts\python.exe main.py
```

---

## 6. 默认账号密码

| 用户名 | 密码 |
|--------|------|
| `0` | `1111` |
| User1 | 1111 |

> 说明：Users.xlsx 中预设了一个测试账号。启动后可直接使用此账号登录，进入主界面查看 User1.xlsx 中的演示账单数据。

---

## 7. 常见错误与解决方法

### 7.1 进入 Python 交互模式导致命令报错

**症状：** 终端出现 `>>>` 提示符，输入 `cd`、`pip`、`python main.py` 等命令报错。

**原因：** 你已经进入了 Python 解释器交互模式（可能因为直接输入了 `python` 回车）。

**解决：**

```python
exit()
```

退出后回到 PowerShell / CMD，再执行原来的命令。

### 7.2 `no Qt platform plugin could be initialized`

**症状：**

```
This application failed to start because no Qt platform plugin could be initialized.
```

**原因：** PyQt5 的 Qt 平台插件（qwindows.dll）加载失败。

常见可能原因：
- 使用的不是 `.venv` 中的 Python（例如误用了系统 Python 3.14）
- PyQt5 安装不完整，缺少 `qwindows.dll`
- Python 版本与 PyQt5 不兼容

**解决方法（按顺序尝试）：**

1. 确认使用的是 `.venv` 中的 Python：

```powershell
where python
```

如果路径不是 `<项目根目录>\.venv\Scripts\python.exe`，请激活虚拟环境：

```powershell
.venv\Scripts\activate
```

2. 检查 `qwindows.dll` 是否存在：

```powershell
python -c "import PyQt5.QtCore, os; p=os.path.join(os.path.dirname(PyQt5.QtCore.__file__), 'Qt5', 'plugins', 'platforms'); print(os.path.exists(os.path.join(p, 'qwindows.dll')))"
```

应输出 `True`。

3. 如果输出 `False`，重装 PyQt5：

```powershell
pip uninstall PyQt5 PyQt5-Qt5 PyQt5-sip -y
pip install PyQt5==5.15.10
```

4. 如果当前 Python 是 3.14，请切换为 Python 3.10 / 3.11 / 3.12 创建虚拟环境。

> main.py 中已内置 Qt 插件路径自动检测，正常情况下无需手动设置环境变量。

### 7.3 `python` 命令指向系统 Python

**症状：** `python --version` 显示 Python 3.14，运行 `python main.py` 报 Qt 插件错误或依赖缺失。

**解决：**

先激活虚拟环境：

```powershell
.venv\Scripts\activate
```

然后再次确认：

```powershell
where python
```

此时应显示 `.venv\Scripts\python.exe`。

### 7.4 `ModuleNotFoundError: No module named 'xxx'`

**症状：**

```
ModuleNotFoundError: No module named 'PyQt5'
ModuleNotFoundError: No module named 'openpyxl'
ModuleNotFoundError: No module named 'matplotlib'
```

**原因：** 依赖未安装。

**解决：**

```powershell
pip install -r requirements.txt
```

---

## 8. 本次复查结果

| 检查项 | 结果 |
|--------|------|
| 关键文件完整性 | 全部存在（main.py、requirements.txt、README.md、.xlsx、.ui 等） |
| .venv Python 版本 | Python 3.12.2 ✓ |
| 依赖导入检查 | PyQt5 + openpyxl + matplotlib 全部通过 ✓ |
| qwindows.dll | 存在 ✓ |
| `python main.py` 启动 | 成功，登录窗口正常显示 ✓ |
| Qt 插件报错 | 无 ✓ |
| 其他 Python 报错 | 无 ✓ |

### 启动方式

```powershell
cd <项目根目录>
.venv\Scripts\python.exe main.py
```

